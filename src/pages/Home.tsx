import {IonContent, IonHeader, IonPage} from '@ionic/react';
import React from "react";

const Home: React.FC = () => {
    return (
        <IonPage>
            <IonHeader>
            </IonHeader>
            <IonContent fullscreen>
            </IonContent>
        </IonPage>
    );
};

export default Home;
